
services:
  system-sentinel:
    build: .
    container_name: production-monitor
    environment:
      - MONITOR_NAME=SUPER-SERVER-V2
      - ALERT_THRESHOLD=95
      - INTERVAL=5
    volumes:
      - ./logs:/app/logs
    restart: always
